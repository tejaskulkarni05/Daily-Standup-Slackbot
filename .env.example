# Slack App Configuration
SLACK_BOT_TOKEN=xoxb-your-bot-token
SLACK_SIGNING_SECRET=your-signing-secret
SLACK_DEFAULT_CHANNEL=C01234567890

# Database Configuration
DATABASE_URL=postgresql+asyncpg://standup_user:standup_password@localhost:5432/standup_db

# Scheduler Configuration
DEFAULT_STANDUP_TIME=09:00
SCHEDULER_TIMEZONE=America/New_York

# Application Configuration
ENV=dev
LOG_LEVEL=INFO
ADMIN_TOKEN=your-secret-admin-token

# Optional: Feature flags
SKIP_NOTIFICATION_TO_CHANNEL=false


https://slack.com/oauth/v2/authorize?client_id=10148862139715.10152040087205&scope=channels:read,chat:write,im:history,im:write,users:read,commands,app_mentions:read&user_scope=


uv run python -c "
import asyncio
from app.db.base import async_session
from app.services.scheduler import dispatch_pending_standups
 
asyncio.run(dispatch_pending_standups(3))
"